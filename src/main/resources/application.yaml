spring:
  rabbitmq:
      host: localhost
      password: guest
      port: 5672
      username: guest
      exchange: mstc.exchange
      routingKey: mstc.routingKey
      queue:
        response: mstc.queue.response
        work: mstc.queue.work
      publisher-confirm-type: correlated
      publisher-returns: true
      template:
        mandatory: true

logging:
  level:
    org:
      springframework: WARN

    mstc: DEBUG